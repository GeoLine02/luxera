"use client";

import Button from "@/app/ui/Button";

const ShopNowButton = () => {
  return (
    <Button
      className="py-3 !w-fit px-12"
      bgColor="dirtyPink"
      rounded="full"
      title="Shop now"
      titleColor="white"
      type="button"
      onClick={() => {}}
    />
  );
};

export default ShopNowButton;
