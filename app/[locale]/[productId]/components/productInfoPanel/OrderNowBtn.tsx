"use client";

import Button from "@/app/ui/Button";

const OrderNowBtn = () => {
  return (
    <Button
      rounded="full"
      title="Order Now"
      type="button"
      bgColor="dirtyPink"
      titleColor="white"
      className="py-1 max-w-[242px]"
      onClick={() => {}}
    />
  );
};

export default OrderNowBtn;
